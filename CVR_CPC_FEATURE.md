# CVR-CPC 关系分析功能说明

## 功能概述

在商品分析系统的投放决策条模块中新增了 CVR-CPC 关系分析功能，帮助用户基于保本边界进行精准的广告投放决策。

## 核心功能

### 1. CVR-CPC 保本边界计算

基于产品的保本广告费，计算转化率(CVR)和点击单价(CPC)的保本边界关系：

- **保本恒等式**: `每单广告费 = CPC / CVR ≤ 保本广告费`
- **边界关系**: `CPC ≤ 保本广告费 × CVR`

### 2. 安全边际分析

实时计算当前 CVR 和 CPC 组合的安全边际：

- **安全边际**: 保本每单广告费 - 当前每单广告费
- **安全状态**: 安全/危险 + 百分比显示
- **边际百分比**: 相对于保本线的安全/超标程度

### 3. 临界值计算

根据已知条件计算临界值：

- **给定 CVR，计算保本 CPC**: `保本CPC = 保本广告费 × CVR`
- **给定 CPC，计算所需最低 CVR**: `最低CVR = CPC / 保本广告费`

## 新增 engine.js 函数

### `breakevenAdCostFromROI(price, returnRate, ROI_BE_effective)`
- **功能**: 根据售价、退货率和保本ROI计算保本广告费
- **参数**: 
  - `price`: 含税售价
  - `returnRate`: 退货率（小数 0-1）
  - `ROI_BE_effective`: 保本ROI（有效营收口径）
- **返回**: 保本广告费（元/单）

### `cpcAtBreakeven(cvr, breakevenAdCost)`
- **功能**: 给定CVR计算保本CPC临界值
- **参数**: 
  - `cvr`: 转化率（小数 0-1）
  - `breakevenAdCost`: 保本广告费（元/单）
- **返回**: 保本CPC临界值（元/点击）

### `cvrAtBreakeven(cpc, breakevenAdCost)`
- **功能**: 给定CPC计算所需最低CVR
- **参数**: 
  - `cpc`: 点击单价（元/点击）
  - `breakevenAdCost`: 保本广告费（元/单）
- **返回**: 保本所需最低CVR（小数 0-1）

### `safetyMarginAnalysis(currentCpc, currentCvr, breakevenAdCost)`
- **功能**: CVR-CPC安全边际分析
- **参数**: 
  - `currentCpc`: 当前CPC（元/点击）
  - `currentCvr`: 当前CVR（小数 0-1）
  - `breakevenAdCost`: 保本广告费（元/单）
- **返回**: 包含安全状态、两种口径边际、临界值等信息的对象
- **新增字段**:
  - `marginPerOrder`: 与保本CPA的差（元/单）
  - `marginPerClick`: CVR-CPC安全边际（元/点击）
  - `cpaDelta`: CPA差值别名
  - `cpcDelta`: CPC差值别名

### `cpcCvrCurve(breakevenAdCost, opts)`
- **功能**: 生成CPC-CVR边界曲线数据点
- **参数**: 
  - `breakevenAdCost`: 保本广告费（元/单）
  - `opts`: 选项 `{cvrMin, cvrMax, steps}`
- **返回**: 边界曲线点数组 `[{cvr, cpc}, ...]`

## 投放决策条新增指标

### CVR-CPC 安全边际
- **显示**: 当前组合相对保本线的安全边际（元/点击）
- **计算**: 保本CPC临界值 - 当前CPC = (保本广告费 × CVR) - CPC
- **示例**: "+0.25元/点击" 表示还有0.25元/点击的安全空间
- **与CPA差的关系**: ΔCPC = CVR × ΔCPA

### CVR-CPC 状态
- **显示**: 当前组合的安全状态和百分比
- **安全状态**: "安全 (+15.2%)" - 绿色显示
- **危险状态**: "危险 (-8.3%)" - 红色显示
- **百分比**: 相对于保本线的安全/超标程度

## 使用场景示例

### 场景1: 保本CPC计算
**已知条件**: CVR = 2%, 保本广告费 = 20元/单
**计算结果**: 保本CPC = 20 × 0.02 = 0.4元/点击
**决策建议**: CPC不超过0.4元即可保本

### 场景2: 所需CVR计算
**已知条件**: CPC = 1.2元/点击, 保本广告费 = 20元/单
**计算结果**: 所需CVR = 1.2 / 20 = 6%
**决策建议**: CVR需达到6%以上才能保本

### 场景3: 安全边际分析
**已知条件**: 当前CPC = 0.8元, 当前CVR = 2.5%, 保本广告费 = 20元/单
**计算过程**:
- 当前每单广告费 = 0.8 / 0.025 = 32元
- 保本每单广告费 = 20元
- 安全边际 = 20 - 32 = -12元
**分析结果**: "危险 (-37.5%)" - 超标12元，需优化CVR或降低CPC

## 口径对齐说明

当选择"有效营收"口径时：
- AOV 自动调整为: `AOV × (1 - 退货率)`
- 保本广告费使用有效营收口径计算结果
- 确保分析结果与实际业务场景一致

## 测试验证

创建了专门的测试文件 `test_cvr_cpc.html` 验证所有函数的正确性：
- 基础计算功能测试
- 边界情况处理测试
- 实际业务场景模拟测试

## 技术实现

### 前端集成
- 在投放决策条的 `calculateDecisionBar()` 函数中集成分析逻辑
- 新增两个KPI指标显示区域
- 实时计算和更新分析结果

### 数据流程
1. 获取当前商品的保本广告费
2. 读取用户输入的CVR和CPC
3. 调用 `safetyMarginAnalysis()` 进行分析
4. 更新UI显示分析结果

### 错误处理
- 参数验证：检查输入参数的有效性
- 边界情况：处理零值、负值等异常输入
- 友好提示：无效输入时显示"—"而非错误

## CVR-CPC 边界可视化图表

### 图表功能
在投放决策条模块下方新增了实时可视化图表，直观展示CVR-CPC保本边界关系：

- **X轴**: 转化率CVR（0-20%，可调整）
- **Y轴**: 点击单价CPC（0-3元，可调整）
- **红色边界线**: 保本临界线 `CPC = 保本广告费 × CVR`
- **绿色安全区域**: 边界线以下的盈利区域
- **蓝色当前点**: 用户输入的CVR和CPC组合
- **网格背景**: 便于读取具体数值

### 交互控制
- **范围调整**: 可自定义CVR和CPC的显示范围
- **实时更新**: 投放参数变化时图表自动刷新
- **重置功能**: 一键恢复默认显示范围
- **图例说明**: 清晰标识各区域和元素含义

### 使用场景
1. **直观判断**: 快速看出当前投放参数是否在安全区域
2. **参数优化**: 通过图表找到最优的CVR-CPC组合
3. **边界探索**: 了解在不同CVR下的最大可承受CPC
4. **趋势分析**: 观察保本边界线的斜率和位置

### 技术实现
- 使用HTML5 Canvas绘制高性能图表
- 实时计算保本边界线方程
- 响应式设计适配不同屏幕尺寸
- 防抖机制优化用户交互体验

## 修复说明

### 问题描述
之前的版本中，"CVR-CPC 安全边际"和"与保本CPA的差"两个指标显示相同的数值，这是因为：
- 两个指标都使用了 `safetyMarginAnalysis()` 函数的 `margin` 字段
- `margin` 字段只返回"元/单"口径的边际值
- 缺少"元/点击"口径的边际值

### 修复方案
扩展 `safetyMarginAnalysis()` 函数，同时返回两种口径的边际：
- **`marginPerOrder`**: 与保本CPA的差（元/单）
- **`marginPerClick`**: CVR-CPC安全边际（元/点击）

### 数学关系
两个边际值的关系：`ΔCPC = CVR × ΔCPA`
- 当 CVR = 1 时，两个值相等
- 当 CVR ≠ 1 时，两个值不同，提供更精准的分析

## 后续扩展建议

1. **图表增强**: 添加历史数据轨迹、多商品对比
2. **批量分析**: 支持多商品CVR-CPC关系对比
3. **历史数据**: 结合历史投放数据进行趋势分析
4. **预警系统**: 当安全边际过低时自动预警
5. **优化建议**: 基于分析结果给出具体的优化建议
