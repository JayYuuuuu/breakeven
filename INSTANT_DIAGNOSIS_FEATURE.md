# 商品分析模块 - 即时诊断功能说明

## 功能概述

在商品分析模块的投放三角分析中新增了"即时诊断"功能，基于 CPC/CVR/客单价 三个核心参数提供全面的投放效果分析和优化建议。

## 主要特性

### 🎯 即时诊断能力
- **完整参数分析**：当输入完整的 CVR、CPC、客单价时，提供全面的投放效果诊断
- **实时计算**：输入变化时自动触发重新计算（防抖处理，避免频繁计算）
- **多口径支持**：支持有效营收口径和GMV口径，与主系统保持一致

### 📊 诊断指标

#### 1. 实际ROI指标
- **实际ROI（有效）**：基于有效营收的投放回报率
- **实际ROI（GMV）**：基于GMV的投放回报率  
- **CPA（每单广告成本）**：CPC ÷ CVR

#### 2. 实际广告占比
- **实际广告占比（有效）**：CPA ÷ 有效营收
- **实际广告占比（GMV）**：CPA ÷ 售价
- **与保本CPA的差**：保本广告费 - 当前CPA（带颜色显示安全/超支状态）

#### 3. 收益与安全指标
- **每点击收益（有效）**：AOV × (1-退货率) × CVR
- **每点击收益（GMV）**：AOV × CVR
- **安全边际（ROI）**：实际ROI / 保本ROI - 1（百分比显示）

### 💡 智能优化建议

#### 敏感度分析
系统自动计算各参数的敏感度，提供精确的优化指导：
- CVR每提升1pp，可承受CPC的提升量
- 客单价每提高1元，可承受CPC的提升量  
- CPC每降低0.1元，等价于CVR的提升量
- CPC每降低0.1元，等价于客单价的提升量

#### 状态评估与建议
- **投放安全**：显示剩余安全空间，建议扩展投放
- **投放超支**：显示超支金额，提供具体优化方向
- **退货率优化**：当退货率较高时，特别提醒其对保本能力的影响

## 使用方法

### 1. 基础操作
1. 在商品分析表格中点击任意商品行的"分析"按钮
2. 在弹出的分析面板中找到"投放三角分析"区域
3. 选择合适的口径（有效营收/GMV）
4. 输入三个关键参数：
   - **转化率(CVR)**：直接输入数字（如2表示2%）
   - **CPC**：每次点击成本（元）
   - **客单价(AOV)**：平均订单价值（元）

### 2. 获取诊断结果
- **临界值计算**：点击"计算临界值"按钮，获取保本边界
- **即时诊断**：点击"即时诊断"按钮，获取全面分析报告

### 3. 自动触发
当三个参数都填写完整后，系统会自动触发即时诊断（800ms防抖延迟）

## 技术实现

### 核心算法
```javascript
// 基础计算
const CPA = CPC / CVR; // 每单广告成本
const ROI_actual = (AOV * effective_rate * CVR) / CPC; // 实际ROI
const safety_margin = (ROI_actual / ROI_breakeven) - 1; // 安全边际

// 敏感度分析
const dCPC_per_1pp_CVR = (AOV / ROI_breakeven) * 0.01;
const dCPC_per_1yuan_AOV = CVR / ROI_breakeven;
```

### 数据流程
1. 获取当前商品的保本参数（售价、成本、退货率等）
2. 计算保本ROI和保本广告费
3. 基于用户输入的CPC/CVR/AOV计算实际指标
4. 对比实际值与保本值，计算安全边际
5. 生成敏感度分析和优化建议
6. 实时更新UI显示

### 口径对齐
- **有效营收口径**：AOV自动调整为 AOV × (1 - 退货率)
- **GMV口径**：AOV保持原值
- 确保分析结果与商品实际业务场景一致

## 使用场景

### 场景1：投放效果评估
**输入**：CVR=2.5%, CPC=1.2元, AOV=79.8元
**输出**：
- CPA = 48元/单
- 与保本CPA的差 = -8元（超支）
- 安全边际 = -15.2%（危险）
- 优化建议：提升CVR至3.1%或降低CPC至0.96元

### 场景2：投放策略优化
**输入**：CVR=3.2%, CPC=0.8元, AOV=85元
**输出**：
- CPA = 25元/单
- 与保本CPA的差 = +5元（安全）
- 安全边际 = +12.5%（安全）
- 扩展建议：可适度提升出价获取更多流量

### 场景3：商品定价决策
通过调整AOV参数，观察对投放安全边际的影响，为商品定价提供数据支持。

## 注意事项

1. **参数完整性**：即时诊断需要填写完整的三个参数
2. **数值有效性**：所有参数必须大于0
3. **口径一致性**：建议与投放决策条使用相同的口径
4. **线性近似**：敏感度分析基于线性近似，适用于小幅调整
5. **实时同步**：参数变化时会自动触发重新计算

## 后续扩展

1. **历史数据对比**：结合历史投放数据进行趋势分析
2. **批量诊断**：支持多商品的批量即时诊断
3. **预警机制**：当安全边际过低时自动预警
4. **A/B测试支持**：支持不同投放策略的效果对比
5. **导出功能**：支持诊断结果的导出和分享

---

该功能与首页的数值分析模块保持一致的计算逻辑，但针对商品分析场景进行了优化，能够基于具体商品的成本结构提供更精准的投放指导。
